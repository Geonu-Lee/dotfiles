source-file ~/.config/tmux/tmux.reset.conf

# PATH 설정 (popup/run-shell에서 tmux 찾을 수 있도록)
set-environment -g PATH "/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin"

set-option -g default-terminal 'screen-256color'
set-option -g terminal-overrides ',xterm-256color:RGB'

set -g mouse on
set -g allow-passthrough all
set -ga update-environment TERM
set -ga update-environment TERM_PROGRAM
set -g prefix ^B

# 마우스 드래그 복사 (yank 플러그인이 처리)
set -g base-index 1              # start indexing windows at 1 instead of 0
set -g detach-on-destroy off     # don't exit from tmux when closing a session
set -g escape-time 0             # zero-out escape time delay
set -g history-limit 1000000     # increase history size (from 2,000)
set -g renumber-windows on       # renumber all windows when any window is closed
set -g set-clipboard on          # use system clipboard
set -g status-position top       # macOS / darwin style
set -g default-terminal "${TERM}"
setw -g mode-keys vi
# Catppuccin Mocha 색상
set -g pane-active-border-style 'fg=#cba6f7,bg=default'  # Mauve
set -g pane-border-style 'fg=#313244,bg=default'          # Surface0

set -g @fzf-url-fzf-options '-p 60%,30% --prompt="   " --border-label=" Open URL "'
set -g @fzf-url-history-limit '2000'

# TPM 설정
set-environment -g TMUX_PLUGIN_MANAGER_PATH '~/.tmux/plugins'
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'
set -g @plugin 'tmux-plugins/tmux-yank'

# Mouse drag copy to clipboard (yank plugin config)
set -g @yank_selection_mouse 'clipboard'
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @plugin 'tmux-plugins/tmux-continuum'
set -g @plugin 'fcsonline/tmux-thumbs'
set -g @plugin 'sainnhe/tmux-fzf'
set -g @plugin 'wfxr/tmux-fzf-url'
set -g @plugin 'omerxx/catppuccin-tmux' # My fork that holds the meetings script bc I'm lazy af
set -g @plugin 'omerxx/tmux-floax'
set -g @floax-width '80%'
set -g @floax-height '80%'
set -g @floax-border-color 'magenta'
set -g @floax-text-color 'blue'
set -g @floax-bind 'p'
set -g @floax-change-path 'true'

# SSH 호스트 선택 (fzf) - 팝업에서 선택 후 새 윈도우로 접속
bind g display-popup -E "/bin/bash $HOME/.config/tmux/scripts/ssh-select.sh"

# tmux 세션 선택 (fzf)
bind s display-popup -E "/opt/homebrew/bin/tmux list-sessions -F '#{session_name}' | /opt/homebrew/bin/fzf --prompt='Session > ' | xargs -I {} /opt/homebrew/bin/tmux switch-client -t {}"

# tmux 세션 생성 (이름 입력)
bind S command-prompt -p "New session:" "new-session -s '%%'"

# pane/window 닫기 (pane 1개면 window 닫기, 아니면 pane 닫기)
bind x if-shell "[ #{window_panes} -eq 1 ]" "kill-window" "kill-pane"

# tmuxinator 워크스페이스 선택 (fzf)
bind w display-popup -E "/bin/bash $HOME/.config/tmux/scripts/tmuxinator-select.sh"

# yazi 파일 매니저 (새 윈도우 - 이미지 프리뷰 지원)
bind f new-window -n "yazi" "/opt/homebrew/bin/yazi"
set -g @continuum-restore 'on'
set -g @resurrect-strategy-nvim 'session'
set -g @catppuccin_window_left_separator ""
set -g @catppuccin_window_right_separator " "
set -g @catppuccin_window_middle_separator " █"
set -g @catppuccin_window_number_position "right"
set -g @catppuccin_window_default_fill "number"
set -g @catppuccin_window_default_text "#W"
set -g @catppuccin_window_current_fill "number"
set -g @catppuccin_window_current_text "#W#{?window_zoomed_flag,(),}"
set -g @catppuccin_status_modules_right "directory date_time"
set -g @catppuccin_status_modules_left "session"
set -g @catppuccin_status_left_separator  " "
set -g @catppuccin_status_right_separator " "
set -g @catppuccin_status_right_separator_inverse "no"
set -g @catppuccin_status_fill "icon"
set -g @catppuccin_status_connect_separator "no"
set -g @catppuccin_directory_text "#{b:pane_current_path}"
set -g @catppuccin_meetings_text "#($HOME/.config/tmux/scripts/cal.sh)"
set -g @catppuccin_date_time_text "%H:%M"

run '~/.tmux/plugins/tpm/tpm'
